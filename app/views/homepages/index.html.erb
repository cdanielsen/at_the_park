<% content_for(:title, "Home | Who's at the Park?") %>
<div class='container'>
  <div class='row text-center'>
    <div class="col-md-12">
      <h1>Who's at the Park?</h1>
    </div>
  </div>
  <div class="row">
    <div class='col-md-6'>
      <h2>Parks</h2>
      <ul>
        <% @parks.all.each do |park| %>
          <li><%= link_to "#{park.name}", "/parks/#{park.id}" %></li>
        <% end %>
      </ul>
    </div>
    <div class='col-md-6'>
      <!-- render visits here? -->
    </div>
  </div>
</div>
