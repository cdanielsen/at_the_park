<% content_for(:title, "#{@park.name} | @ThePark!") %>

<div class = "row">
  <div class = "col-md-4">
    <h2>Edit park</h2>
    <%= render('layouts/errors', :object => @park) %>
    <%= render('form') %>
  </div>
</div>
